import { DirectivasDirective } from './directivas.directive';

describe('DirectivasDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivasDirective();
    expect(directive).toBeTruthy();
  });
});
